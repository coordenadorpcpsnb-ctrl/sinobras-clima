# Dependências extras da Fase 2A (C3S) — não fazem parte do pipeline de
# produção (requirements.txt) nem das Fases 1.x. Instaladas nesta sessão
# via `pip install cdsapi xarray cfgrib`; documentadas aqui separadamente
# para não confundir com as dependências de produção.
#
# NOTA: instalar isso no MESMO ambiente das Fases 1.x atualizou numpy
# (>=1.24 -> 2.4.6) e pandas (>=2.0 -> 3.0.5) como dependências
# transitivas — verificado nesta sessão que a suíte de testes completa
# (139 testes, Fases 1/1.1/1.2/1.3) continua passando com essas versões
# mais novas, mas isso não é o mesmo que garantir que não há nenhuma
# diferença sutil de comportamento fora do que os testes cobrem. Rodar
# num ambiente virtual separado (`python -m venv .venv-c3s`) é a forma
# mais segura de evitar esse acoplamento numa fase futura.
cdsapi>=0.7
xarray>=2026.1
cfgrib>=0.9
eccodes>=2.48

# Fase 2C.1 (NMME) — xarray abre NetCDF via este backend (netCDF4), em
# vez de cfgrib/eccodes (GRIB, só C3S). Já estava presente no ambiente
# desta sessão como dependência transitiva; declarado aqui explicitamente
# porque nmme_poc.py depende dele e o workflow instala este arquivo num
# runner limpo.
netCDF4>=1.6

# Fase 2C.1b (revisão pós-execução #1) — a execução real #1 (run
# 35872475562) falhou ao abrir o NetCDF do CFSv2 com
# "unable to decode time units 'months since 1960-01-01' with calendar
# '360'. Try opening your dataset with decode_times=False or installing
# cftime" — calendário 360_day (comum em modelos climáticos) não é
# decodificável pelo xarray sem cftime. Não estava declarado aqui antes
# porque não tinha aparecido como necessário até essa execução real
# (Seção 1). NUNCA remove o suporte padrão de decode_times=True — só
# garante que ele funcione para calendários não-gregorianos; o fallback
# decode_times=False (nmme_poc.abrir_dataset_com_fallback_temporal)
# continua existindo como segunda linha de defesa, não como substituto.
cftime>=1.6.3

# Usado só por tests/test_c3s_poc.py para validar a estrutura do YAML
# do workflow (on.workflow_dispatch.inputs, ausência de cron) — não é
# dependência de produção nem do restante do pipeline C3S. Declarado
# aqui explicitamente porque o workflow instala requirements.txt +
# requirements-c3s.txt num runner limpo, que não tem nada instalado
# "incidentalmente" (ao contrário de um ambiente de desenvolvimento
# local onde PyYAML costuma já estar presente por outra dependência).
PyYAML>=6
